<!DOCTYPE html>
<html>
<head>
    <title><%= movie.title %></title>
</head>
<body>

<h1><%= movie.title %> (<%= movie.year %>)</h1>

<p><%= movie.description %></p>

<h2>Reviews</h2>

<% if (movie.reviews.length === 0) { %>
    <p>No reviews yet.</p>
<% } else { %>
    <ul>
        <% movie.reviews.forEach(r => { %>
            <li>
                <strong><%= r.username %></strong> — Rating: <%= r.rating %>
                <p><%= r.comment %></p>
            </li>
        <% }) %>
    </ul>
<% } %>

<hr>

<% if (!userReviewed) { %>
<h3>Add Review</h3>
<form action="/api/movies/<%= movie.id %>/reviews" method="POST">
    <label>Rating (1-5):</label>
    <input type="number" name="rating" min="1" max="5" required><br><br>

    <textarea name="comment" placeholder="Your review..." required></textarea><br><br>

    <button type="submit">Submit Review</button>
</form>
<% } else { %>
<p>You already reviewed this movie.</p>
<% } %>

<p><a href="/movies">Back to movies</a></p>

</body>
</html>
